// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

// datasource db {
//   provider = "postgresql"
//   url      = env("DATABASE_URL")
// }

datasource db {
  provider = "sqlite"
  url = "file:./dev.db"
}

model TodoItem {
  id String @id @default(cuid()) // collision resistent unique id 
  item String @unique
  date String
  due String
  done Boolean @default(false)
}

// npx prisma studio
// starts studio at localhost:5555
// studio is a database management tool 